fastapi==0.115.6
uvicorn==0.34.0
httpx==0.28.1
python-dotenv==1.0.1
pydantic==2.10.4
sentence-transformers==3.3.1
motor==3.6.0
pymongo>=4.9,<4.10

# Testing deps live in requirements-test.txt, not here — the runtime Docker
# image (backend/Rag_Chatbot/Dockerfile) only installs this file, so pytest/
# mongomock-motor/respx never ship in the deployed image.