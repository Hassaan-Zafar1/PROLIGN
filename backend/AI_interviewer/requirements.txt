groq>=0.9.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
pymongo>=4.7.0
python-multipart>=0.0.9

# Testing deps live in requirements-test.txt, not here — the runtime Docker
# image (backend/AI_interviewer/Dockerfile) only installs this file, so
# pytest/mongomock/respx never ship in the deployed image.